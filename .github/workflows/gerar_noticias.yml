name: Gerar Notícias Diárias

on:
  workflow_dispatch: # Permite que você inicie a automação manualmente pelo site do GitHub
  schedule:
    - cron: '0 8 * * *' # Roda automaticamente todos os dias às 8:00 UTC (5:00 da manhã no horário de Brasília)

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout do repositório
        uses: actions/checkout@v4

      - name: Configurar Python 3.10
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Instalar dependências
        run: |
          python -m pip install --upgrade pip
          pip install requests

      - name: Rodar o script de geração de notícias
        env:
          HUGGINGFACE_API_TOKEN: ${{ secrets.HUGGINGFACE_API_TOKEN }}
        run: python gerador_de_noticias.py

      - name: Fazer commit do novo site com o Ajudante Especializado
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "📰 Atualiza notícias do dia"
          file_pattern: index.html # Apenas salva se o arquivo index.html for criado ou modificado

